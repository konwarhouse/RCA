# UNIVERSAL PROTOCOL STANDARD ENVIRONMENT VARIABLES
# All timeout and configuration values must use these instead of hardcoding

# Frontend timeout configurations
VITE_REFRESH_INTERVAL=3000
VITE_ANALYSIS_DURATION=3000
VITE_FALLBACK_DURATION=2000
VITE_INTEGRATION_DURATION=3000
VITE_HYPOTHESIS_DURATION=2000
VITE_VALIDATION_DURATION=2000
VITE_AUTOSAVE_INTERVAL=30000
VITE_REPORT_GENERATION_DELAY=3000
VITE_DEFAULT_TIMEOUT=3000
VITE_SAVE_STATUS_TIMEOUT=2000
VITE_REDIRECT_DELAY=2000
VITE_AUTOSAVE_DELAY=2000

# Backend configuration
VITE_BACKEND_PORT=5000

# Database configuration (provided by Replit)
DATABASE_URL=your_database_url_here

# AI Configuration (managed through admin panel - DO NOT HARDCODE)
# These are set dynamically through the admin interface:
# - Provider selection (OpenAI, Anthropic, etc.)
# - API keys (encrypted in database)
# - Model selection (per provider)
# - Active status (admin controlled)

# Session configuration
SESSION_SECRET=your_session_secret_here